#!/bin/bash -e

installMeson() {
  local version=$1
  local mesonVersion=${version##-*}
  echo "Installing Meson version: ${mesonVersion}"
  python3 -m pip install meson==${mesonVersion}
}

if [ "$1" = "configure" ]; then
  installMeson "$2"
fi
