#!/bin/bash -e

#DEBHELPER#

PACKAGE_VERSION="#ENV.PACKAGE_VERSION#"

installMeson() {
  local mesonVersion="${PACKAGE_VERSION%~*}"
  pip3 install --no-warn-script-location meson=="${mesonVersion}"
}

if [ "$1" = "configure" ]; then
  installMeson
fi
